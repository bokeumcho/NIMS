name: py312cu121
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.12
  - pytorch           # installs the GPU-enabled build selected by pytorch-cuda
  - pytorch-cuda=12.1 # CUDA runtime bundled with PyTorch
  - numpy
  - tqdm
  - matplotlib
